<select
        name="{{ field_type.getInputName() }}"
        class="form-control select2"
        data-placeholder="{{ field_type.placeholder }}"
        multiple
        >

    {% for option in field_type.options %}

        <option value="{{ option.value }}" {{ option.selected ? 'selected' }}>{{ option.title }}</option>

    {% endfor %}

</select>
