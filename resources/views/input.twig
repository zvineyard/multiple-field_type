<select name="{{ name }}" class="{{ class }} form-control select2" data-placeholder="{{ placeholder }}" multiple>

    {% for option in options %}

        <option value="{{ option.value }}" {{ option.selected ? 'selected' }}>{{ option.title }}</option>

    {% endfor %}

</select>